using UnityEngine;

public class GameManager : MonoBehaviour
{
    private bool isHeadTilt = false;
    //Chưa xử lý nâng cằm
    private bool isMouthOpen = false;
    private bool hasDoneBreathing = false;
    private string defaultText = "Tay phải ấn nút Grab để ngửa đầu. \nTay trái ấn nút X để nâng cằm và há miệng.";
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
